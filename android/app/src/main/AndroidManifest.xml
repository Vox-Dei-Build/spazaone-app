<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for taking photos with ImagePicker -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Gallery selection on Android 13+ uses the system Photo Picker
         (ACTION_PICK_IMAGES), which deliberately does NOT require
         READ_MEDIA_IMAGES / READ_MEDIA_VIDEO. Declaring those broad
         permissions just to call the picker triggers Google Play's
         Photo and Video Permissions policy gate and forces a
         justification submission, even though they're never actually
         needed for our flow (we only pick a single image at a time
         from the picker; we never enumerate the user's library).
         image_picker 1.0+ on image_picker_android 0.8.12+11+ routes
         pickImage(source: gallery) through Photo Picker automatically
         on API 33+, so dropping these permissions is purely
         beneficial: no Play policy gate, no permission prompt, same
         user-visible behaviour. See `lib/utils/photo_upload_util.dart`
         which now skips PermissionHelper.requestPhotos for the gallery
         source on Android. -->
    <!-- Legacy storage permission for Android 12 and below. The
         maxSdkVersion bound makes this a no-op on API 33+ where Photo
         Picker (above) handles gallery selection without any
         permission. On API 32 and below, image_picker still uses the
         classic ACTION_GET_CONTENT path which needs this permission. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 11+ (API 30+) requires explicit declaration of every
         external app/scheme the app intends to query or launch via
         PackageManager.resolveActivity / Intent.resolveActivity, which
         underpins url_launcher.canLaunchUrl(). Without these entries
         canLaunchUrl returns false silently and url_launcher refuses
         to launch even when the target app IS installed, breaking:
           - "Top up via WhatsApp" button (https://wa.me/...)
           - WhatsApp share button (whatsapp://send?...)
           - "Call instead" fallback (tel:...)
           - Email composer fallback (mailto:...)
           - External browser links (https://...) -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
    </queries>


    <application
        android:label="SpazaOne"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher" />

        <!-- PostHog: disable auto-init so we can gate the SDK on user consent.
             TelemetryService.init() performs Posthog().setup() manually. -->
        <meta-data
            android:name="com.posthog.posthog.AUTO_INIT"
            android:value="false" />

        <!-- Firebase Crashlytics: keep enabled but defer collection until
             CrashService confirms consent at runtime via setCrashlyticsCollectionEnabled. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- Firebase Analytics: start disabled. TelemetryService flips this at
             runtime only after ConsentService has a saved analytics decision. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
            />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Removed Branch Deeplinking intent filters -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="payment-success" />
            </intent-filter>
        </activity>

        <!-- Removed Branch Live Deeplinking configuration -->


        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
